package com.senac.projeto4.entity;

// Enum que lista todos os papéis (perfis de acesso) possíveis no sistema.
public enum RoleName {
    // Papel principal para o administrador de um restaurante específico.
    ROLE_ADMINISTRATOR,

    // Exemplo de outro papel que poderia existir (e.g., um papel para o atendente)
    ROLE_MANAGER
}